import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { services, serviceAreas, site } from "@/lib/site";
export function generateStaticParams(){return serviceAreas.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params; const a=serviceAreas.find(x=>x.slug===slug); if(!a)return {}; return {title:`Mobile Detailing in ${a.name}, NV`, description:`Premium mobile auto detailing, ceramic coatings, full details, maintenance plans, and add-ons in ${a.name}, Nevada.`}}
export default async function AreaPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const a=serviceAreas.find(x=>x.slug===slug); if(!a) notFound(); return <main className="page"><section className="page-hero"><p className="eyebrow">Service Area</p><h1>Mobile Detailing in {a.name}</h1><p>{a.intro} Dripped Auto Detailing brings professional care directly to your home, workplace, or appointment location.</p><a className="btn" href={site.smsHref}>Get Quote in {a.name}</a></section><section className="content-grid"><div className="content-card"><h2>Premium mobile detailing in {a.name}</h2><p>Drivers in {a.name} deal with the same Las Vegas conditions that make regular detailing important: dust, sun exposure, hot interiors, brake dust, bugs, and road grime. Dripped Auto Detailing is built around quality, precision, and mobile convenience so you can keep your vehicle clean without wasting time at a shop.</p><p>Popular services in {a.name} include full details, exterior SIO3 packages, interior restore details, and Gyeon Certified ceramic coatings for new and premium vehicles.</p></div><div className="content-card"><h2>Services available</h2><ul>{services.slice(0,8).map(s=><li key={s.slug}><Link href={`/services/${s.slug}`}>{s.name}</Link></li>)}</ul></div></section><iframe className="map" src={site.mapsEmbed} loading="lazy" title={`Dripped Auto Detailing map for ${a.name}`} /></main>}
