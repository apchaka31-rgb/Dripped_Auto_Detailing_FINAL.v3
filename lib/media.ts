export const galleryImages: { src: string; alt: string; category: string }[] = [
  // Add real optimized images in /public/gallery, then add entries here.
];

export const reviewScreenshots: { src: string; alt: string }[] = [
  // Add Google review screenshots in /public/reviews, then add entries here.
];

export const beforeAfterImages: { before: string; after: string; alt: string }[] = [
  // Add before/after images in /public/before-after, then add entries here.
];
